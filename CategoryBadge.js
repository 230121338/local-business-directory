export default function CategoryBadge({ name }) {
  return (
    <span className="inline-block px-2 py-0.5 text-xs font-mono uppercase tracking-wide text-clay bg-clay/10 rounded-stall">
      {name}
    </span>
  );
}
